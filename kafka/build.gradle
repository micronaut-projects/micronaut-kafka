plugins {
    id 'io.micronaut.build.internal.kafka-module'
}

dependencies {
    annotationProcessor libs.micronaut.graal

    api libs.managed.kafka.clients

    compileOnly libs.micronaut.micrometer
    compileOnly libs.opentracing.kafka.client
    compileOnly libs.zipkin.brave.kafka.clients
    compileOnly libs.micronaut.graal
    compileOnly libs.graal

    testImplementation libs.micronaut.rxjava2
    testImplementation libs.micronaut.http.client
    testImplementation libs.opentracing.mock
    testImplementation libs.opentracing.kafka.client
    testImplementation libs.micronaut.rxjava2

    testRuntimeOnly libs.micronaut.micrometer.statsd
    testRuntimeOnly libs.micronaut.tracing
}

//testlogger {
//    showStandardStreams true
//}
